# Ap4 – Eigen API-experiment 2 (Python) met webforms

## Doel
Een eenvoudige Flask webapp met webforms gebruiken om data te verwerken en resultaten te tonen (API/web layer).

## Context
- Tooling: Python, Flask
- UI: HTML templates (`templates/`) + CSS (`static/style.css`)
- Data: SQLite (via `db.py`)

## Werkwijze (kort)
1. Flask app starten.
2. Formulier invullen (signup/login/update).
3. Data wordt verwerkt en opgeslagen/gelezen uit SQLite.

## Run (voorbeeld)
```bash
python3 flask_app_login.py
