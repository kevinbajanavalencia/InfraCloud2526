# Js4 – Conversie van JSON naar andere formaten

## Doel
JSON-data omzetten naar een ander dataformaat (YAML) om het verschil in structuur/leesbaarheid aan te tonen.

## Context
- Tooling: Python (notebook) / JSON / YAML
- Input: `computers.json`
- Output: `output.yaml`

## Werkwijze (kort)
1. JSON inlezen.
2. Conversie uitvoeren naar YAML.
3. Output wegschrijven naar bestand.

## Run (voorbeeld)
- Open het notebook (bv. Jupyter) en voer de cellen uit.

## Resultaat / bewijs
- `output.yaml` bevat dezelfde informatie als `computers.json` in YAML-formaat.

## Reflectie
- YAML is vaak leesbaarder voor configuratiebestanden.
- JSON is strikter en komt veel voor in API-responses.
